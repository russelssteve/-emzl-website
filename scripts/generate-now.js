const DailyContentGenerator = require('./generate-daily-content');\nconst SocialMediaPublisher = require('./social-media-publisher');\n\nasync function generateNow() {\n  console.log('🚀 Génération immédiate d\\'un article...');\n  \n  try {\n    // Générer l'article\n    const generator = new DailyContentGenerator();\n    const article = await generator.generate();\n    \n    if (article) {\n      console.log('✅ Article généré avec succès');\n      \n      // Publier sur les réseaux sociaux\n      const publisher = new SocialMediaPublisher();\n      await publisher.publishLatestArticle();\n      \n      console.log('🎉 Processus terminé avec succès!');\n    } else {\n      console.log('❌ Échec de la génération d\\'article');\n    }\n  } catch (error) {\n    console.error('❌ Erreur:', error);\n  }\n}\n\ngenerateNow();